extends layout

block content
  .container
    .clearfix
      form.form-signin(action='/signin', method='POST')
        h3.form-signin-heading Signin
        label.sr-only(for='email') Email address
        input#email.form-control(type='email', name='email', placeholder='Email address', required='required', autofocus='autofocus')
        label.sr-only(for='password') Password
        input#password.form-control(type='password', name='password', placeholder='Password', required='required', autofocus='autofocus')

        button.btn.btn-primary.btn-block(type='submit') Signin

        a.btn.btn-dark.btn-block(href='/auth/facebook')
          i.fa.fa-facebook.fa-lg
          |         Signin with Facebook
